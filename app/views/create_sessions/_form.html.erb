<%= form_for @create_session, html: { multipart: true } do |f| %>
	<% if @create_session.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@create_session.errors.count, "error") %> prohibited this session from being saved:</h2>

			<ul>
			<% @create_session.errors.full_messages.each do |messages| %>
				<li><%= message %></li>
			<% end %>
			</ul>
		</div>

		<div class="field">
			<%= f.label :image %><br>
			<%= f.file_field :image %>
		</div>
		<div class="field">
			<%= f.label :title %><br>
			<%= f.text_field :title %>
		</div>
		<div class="field">
			<%= f.label :content %><br>
			<%= f.text_area :content %>
		</div>
		<div class="actions">
			<%= f.submit %>
		</div>

<% end %>

